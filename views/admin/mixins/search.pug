mixin search()
    form(id="form-search")
        .input-group 
            input(
                type="text"
                placeholder="Type key"
                name="keyword"
                class="form-control"
                value= keyword
            )
            .input-group-append 
                button(
                    class="btn btn-success"
                    type="submit"
                ) Find